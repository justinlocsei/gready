#!/bin/bash

origin="${BASH_SOURCE[0]}"
script_path="$(readlink "$origin" || echo "$origin")"
root_dir="$(cd "$(dirname "$script_path")/.." && pwd)"

cd "$root_dir" || exit 1

./node_modules/.bin/ts-node src/index.ts "$@"
